PKG_CFLAGS = @ADDITIONAL_FLAGS@
PKG_LIBS = @ADDITIONAL_FLAGS@

FS_OBJECTS = filesampler/file_sampler.o filesampler/wc.o
R_OBJECTS = filesampler_native.o wrappers.o
OBJECTS = $(FS_OBJECTS) $(R_OBJECTS)

all: $(SHLIB)

$(SHLIB): $(OBJECTS)
